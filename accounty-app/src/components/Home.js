import React from 'react';
import { NavLink } from 'react-router-dom';

const Home = () => {
    return (
        <dive>
            <NavLink to="/">Home</NavLink>
        </dive>
    );
}

export default Home;